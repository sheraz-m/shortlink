# Shortlink

A simple URL shortener built with Flask + Postgres.

---

## Run locally (Docker)

### Requirements
- Docker Desktop

### Start
```bash
git clone https://github.com/sheraz-m/shortlink.git
cd shortlink
docker compose up --build
